import React from "react";
import styled from "styled-components";

function Chat() {
  return <ChatDiv>ch</ChatDiv>;
}

const ChatDiv = styled.div`
  display: flex;
  flex-direction: column;
  flex: 0.65;
`;

export default Chat;
